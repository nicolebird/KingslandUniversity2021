//line 2 through 13 for one problem // line 2-5 then line 15 thru 17 for another  // line 2-5 and 19-22 is a third problem
let person = {
    age: 25,
};
    person.name = "Tom";
    
    // let personArray = Object.entries(person);

    // let personProperties = Object.keys(person);
    // let personValues = Object.values(person);

    // console.log(personProperties[0]);
    // console.log(personValues[0]);

    // for(let key in person){
    //     console.log(`${key}: ${person[key]}`);
    // }

    let array = [1,2,3,4,5];
    for(let index in array){
        console.log(index);
    }
