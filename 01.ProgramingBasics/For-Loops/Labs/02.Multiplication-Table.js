function main(number) {
	for (let i = 1; i <= 7; i++) {
		// multiply i with number
		const result = i * number;

		// display the result
		console.log(`${number} * ${i} = ${result}`);
	}
}

main(2);

//not in homework grader yet
