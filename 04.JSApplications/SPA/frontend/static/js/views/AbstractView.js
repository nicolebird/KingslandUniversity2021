export default class {
    constructor(params){
        //add stuff below.  (did that per instruction)
        this.params = params;
    }

    setTitle(title){
        document.title = title;
    }

    async getHtml(){
        return "";
    }
}